import { withInstall } from '@/components/install'

import ArticleWrap from './index.vue'

export const BaseArticleWrap = withInstall(ArticleWrap)
export default BaseArticleWrap

export * from './feed-back.vue'
